# Python dependencies for the RA-Skills data and chart tiers.
#
# The CATALOG tier (skills/imf-ra-catalog) and the WEO country-group helpers use
# only the Python 3.9+ standard library plus the CSVs bundled in this repo — they
# need NOTHING from this file and run anywhere.
#
# The DATA tier (skills/imf-ra-data: fetch_idata.py / fetch_haver.py) needs:
pandas>=1.3
openpyxl>=3.0      # Excel (.xlsx) engine used by the refreshable / --excel output paths
#
# The CHART tier (skills/imf-ra-charts) needs:
matplotlib>=3.5    # PNG chart generation for skills/imf-ra-charts
plotly>=5.0        # optional self-contained interactive HTML chart output
xlsxwriter>=3.0    # optional editable Excel chart workbook output
#
# NOT pip-installable — install separately on an IMF-managed machine:
#   imf_datatools   internal IMF SDK; install with:
#                     python \\ecnswn12p\ems_shared\pub\datatools\installer.py dev
#                   then restart the Python environment. See:
#                     skills/imf-ra-data/references/imf_datatools_agent_api_reference.md (section 1.1)
#
# See AGENTS.md (Dependencies & environments) for which tiers run where.
